# core/src

Implementation for the Rust classifier.

- `lib.rs`: event definitions and classification logic
- `main.rs`: command-line interface that reads lines and emits JSON events

See `../README.md` for architecture and classifier flow.
